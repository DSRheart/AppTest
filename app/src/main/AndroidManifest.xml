<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dashan.apptest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".test.Test2Activity"
            android:label="@string/title_activity_test2"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".test.TestAPPActivity"
            android:label="@string/title_activity_test_app"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".tab_viewpager.TabActivity"
            android:label="@string/title_activity_tab">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewapger.PageViewActivity" />
        <activity android:name=".recycler.ListActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".evenbus.EvenBus2Activity" />
        <activity android:name=".evenbus.EvenBus1Activity" />
        <activity android:name=".mvp.MvpActivity" />
    </application>

</manifest>